# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant::Config.run do |config|
  config.vm.box = "startonrails"
  config.vm.network :hostonly, "192.168.50.2"
  config.vm.share_folder "v-root", "/Users/fnando/Desktop/movies-app", "."
end
